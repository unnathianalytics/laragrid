{
    "name": "@unnathianalytics/laragrid-build",
    "private": true,
    "description": "Package-development build tooling only \u2014 consumers never run this. Bundles resources/js + resources/css into the committed dist/ assets.",
    "type": "module",
    "scripts": {
        "build": "node build/build.mjs",
        "watch": "node build/build.mjs --watch",
        "test": "node tests/js/run-expression-vectors.mjs && node tests/js/run-lru-vectors.mjs && node tests/js/run-nav-vectors.mjs && node tests/js/run-picker-vectors.mjs"
    },
    "devDependencies": {
        "esbuild": "^0.28.1"
    }
}
